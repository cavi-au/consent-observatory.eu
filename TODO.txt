




DEPLOY TIL webservice2 server og se om vi kan få det til at virke der, vi skal nok have åbnet for mail-ud, installere puppeteer ui-libs og så
sætte substi for url-resolve til sveltekit, så url'er virker og så lave apache reverse proxy /consent-observatory.eu -> localhost:7098
Få lavet en række dev1 - dev6 domæner der peger webservice2 serveren...

Se mails med TODO's og tjek todo liste på tlf,
Se TODO's i projekt

Kan vi lave "select" "unselct" som links i grå farve under deres heading, og si en størrelse 12px
padding i toppen skal væk på mobil
titel på forside skal skalere responsivt ligesom h1

Layout midas/peter
- instead of setting a lot of sizes to 70%, shouldn't we just set the max page size to 960 or 840 instead of 1200?
- Images does not with current styling behave well when resizing the browser
- buttons especially for the "select all / unselect all" could use some love
- there is a lot of space at the top of pages with text, at least make i adapt when scaling down?
- There is nowhere to find out what the analysis actually does, why would you do it?
- use boostrap default classes like "text-center" instead of "center-text" and "centering-parent" etc.
- clean up stylesheet for unused styles



